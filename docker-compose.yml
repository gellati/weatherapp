version: '2'
services:
  frontend:
    container_name: weatherapp_frontend
    build: ./frontend
    ports:
      - "8000:8000"
    depends_on:
      - backend
  backend:
    container_name: weatherapp_backend
    build: ./backend
    ports:
      - "9000:9000"
